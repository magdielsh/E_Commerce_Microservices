# -----------------------------------------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------------------------------------
server:
  # Puerto del servicio
  port: 8183
  # Servlet context path (si necesitamos /api prefix)
  # servlet:
  #   context-path: /api

# -----------------------------------------------------------------------------
# SPRING APPLICATION - CONFIGURACI�N PRINCIPAL
# -----------------------------------------------------------------------------
spring:
  data:
    redis:
      host: localhost
      port: 6379
      password: postgres
      timeout: 2000ms
  # Application name (para service discovery, logs, etc.)
  application:
    name: notification-service

  #EMAIL (Gmail)
  mail:
    host: smtp.gmail.com
    port: 587
    username: pedritos881@gmail.com
    password: ${SPRING_MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  kafka:
    # ─── Conexión al broker ───────────────────────────────────────────
    bootstrap-servers: localhost:9092        # (1)

    # ─── CONSUMIDOR ──────────────────────────────────────────────────
    consumer:
      group-id: notification-processing-consumer       # (10)
#      auto-offset-reset: earliest            # (11)
#      enable-auto-commit: false              # (12)
#      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer   # (13)
#      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
#      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer # (14)
#      properties:
#        spring.json.value.default.type: "com.e_commerce.notificationservice.Dto.UserCreatedValidationEvent"
#        spring.json.use.type.headers: false
#        max.poll.records: 10                 # (16)
#        session.timeout.ms: 30000
      # (17)
# -----------------------------------------------------------------------------
# EUREKA SERVICE DISCOVERY
# -----------------------------------------------------------------------------
eureka:
  # Client configuration
  client:
    # Habilitar registro en Eureka
    enabled: true
    # URL del Eureka Server
    service-url:
      defaultZone: http://localhost:8761/eureka/
    # Registry fetch interval (seconds)
    registry-fetch-interval-seconds: 30
    # Instance preference
    instance-info-replication-interval-seconds: 30
  # Instance configuration
  instance:
    # Prefer IP address (vs hostname)
    prefer-ip-address: true
    # Instance ID en Eureka
    instance-id: ${spring.application.name}:${server.port}
    # Lease renewal interval (seconds)
    lease-renewal-interval-in-seconds: 30
    # Lease expiration duration (seconds)
    lease-expiration-duration-in-seconds: 90


# -----------------------------------------------------------------------------
# LOGGING CONFIGURATION
# -----------------------------------------------------------------------------
logging:
  # Level root
  level:
    root: INFO
    com.banco.sistema: DEBUG
  # Pattern
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

app:
  base:
    url: http://localhost:7080